<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
<style name="radioButton">
    <item name="android:layout_marginBottom">@dimen/dim_8dp</item>
    <item name="android:layout_marginEnd">@dimen/dim_8dp</item>
    <item name="android:layout_marginStart">@dimen/dim_8dp</item>
    <item name="android:textColor">@android:color/white</item>
    <item name="android:buttonTint">@color/textColor1</item>
</style>
    <style name="TextView">
        <item name="android:layout_margin">@dimen/dim_8dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/dime_18sp</item>
    </style>

</resources>
